- if logged_in?
  - if @current_company
    = link_to('Users', users_path(:subdomain => @subdomain), :class => 'admin', :style => 'color: #DDDDDD;')
    == &nbsp;|&nbsp;
  Logged in as
  = link_to("#{current_user.email}", edit_user_path(current_user, :subdomain => @subdomain), :class => 'admin', :style => 'color: #DDDDDD;')
  == &nbsp;|&nbsp;
  = link_to('Log-out', logout_path, :class => 'admin', :style => 'color: #DDDDDD;')
- else
  = link_to('Log-in', login_path, :class => 'admin', :style => 'color: #DDDDDD;')

