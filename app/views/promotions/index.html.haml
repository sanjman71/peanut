- title('Promotions')

.span-23.prepend-1.mat.padding-top-10.padding-bottom-10
  %h2== Promotions
  %h5= link_to 'Create', new_promotion_path

%hr.space

.span-23.prepend-1
  .span-11
    %h5= page_entries_info(@promotions)
  .span-5.last
    = will_paginate(@promotions, :page_links => false)

%hr.space

- @promotions.each do |promotion|
  .span-20.prepend-1.padding-top-10[promotion]{:class => cycle("shaded", "")}
    .span-4
      %h2= promotion.code
    .span-4
      %h5{:style => 'font-size: 1.1em;'}= promotion_discount_to_s(promotion)
    .span-4
      %h5{:style => 'font-size: 1.1em;'}= promotion_uses_to_s(promotion)
    .span-4
      %h5= promotion_expires_to_s(promotion)
