<h1><%= @current_company.name %>: Appointments</h1>

<table>
  <tr>
    <th>Company</th>
    <th>Job</th>
    <th>Resource</th>
    <th>Start at</th>
    <th>End at</th>
  </tr>

<% for appointment in @appointments %>
  <tr>
    <td><%=h appointment.company_id %></td>
    <td><%=h appointment.job.name %></td>
    <td><%=h appointment.resource.name %></td>
    <td><%=h appointment.start_at %></td>
    <td><%=h appointment.end_at %></td>
    <td><%= link_to 'Show', appointment %></td>
    <td><%= link_to 'Edit', edit_appointment_path(appointment) %></td>
    <td><%= link_to 'Destroy', appointment, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New appointment', new_appointment_path %>
