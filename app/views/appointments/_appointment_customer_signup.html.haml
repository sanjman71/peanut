.span-23.prepend-1.last#rpx_login{:style => 'padding-top: 20px;'}
  .span-13#rpx_details
    == Select a provider to the right and login using your existing credentials.
    %br
    - if signup == :all
      -# allow peanut logins in addition to rpx
      %span{:style => 'color: #777777; font-size: 0.8em; padding: 5px 0px;'}== or
      %br
      == Click
      = link_to 'here', '', :id => 'show_peanut_login'
      == to create a new account.
  .span-10.last
    #rpx_login_loading{:style => 'font-size: 1.0em; font-weight: bold;'}== Login Form Loading ...
    = RPXNow.embed_code('walnut', rpx_login_url)

.last#peanut_login.hide
  .span-22.prepend-1.last{:style => 'padding-top: 20px;'}
    == Please enter your name, email, and password below to confirm your appointment and create your account.
    %br
    == Your appointment confirmation will be emailed to you.

  .span-15.last
    .span-3.label.text-align-right{:style => 'padding-top: 20px;'}
      == Name:
    .span-12.last
      = text_field_tag 'customer[name]', '', :id => 'customer_name', :class => 'title required', :style => 'font-size: 1.2em; width: 400px;'

  .span-15.last
    .span-3.label.text-align-right{:style => 'padding-top: 20px;'}
      == Email:
    .span-12.last
      = text_field_tag 'customer[email]', '', :id => 'customer_email', :class => 'title required', :style => 'font-size: 1.2em; width: 400px;'

  .span-15.last
    .span-3.label.text-align-right{:style => 'padding-top: 20px;'}
      == Password:
    .span-12.last
      = password_field_tag 'customer[password]', '', :id => 'customer_password', :class => 'title required', :style => 'font-size: 1.2em; width: 400px;'

  .span-15.last
    .span-3.label.text-align-right{:style => 'padding-top: 20px;'}
      == Confirm:
    .span-12.last
      = password_field_tag 'customer[password_confirmation]', '', :id => 'customer_password_confirmation', :class => 'title required', :style => 'font-size: 1.2em; width: 400px;'

  .span-14.prepend-1.last
    = link_to 'Login using an existing account', '', :id => 'show_rpx_login', :style => 'font-size: 0.8em;'

  .span-15.last
    .span-3.label
      == &nbsp;
    .span-12.last
      %em.hide#customer_email_validate_message== &nbsp;
