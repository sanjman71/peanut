if @redirect
  page.redirect_to(@redirect)
else
  page["#progress"].hide();  # Hide progress bar
  page["#submit"].show();    # Show submit button 
  page["#appointment_code"].addClass('highlighted')
  page["#appointment_code"].focus()
end