- javascript('appointments')

.span-24
  .span-23.prepend-1.mat.padding-bottom-10
    %h2== My Appointments
    
  %hr.space
  
  .span-23.prepend-1
    %h3== #{pluralize(@my_appointments.size, 'appointment')}
    
  = render(:partial => 'customer_appointments', :locals => {:appointments => @my_appointments})