# add flash notice
page[:flash].addClassName 'notice'
page[:flash].replace_html @notice_text

# update services
page[:services_list].replace_html :partial => 'services.html.haml', 
                                  :locals => {:services => @services}
