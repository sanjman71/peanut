# rebuild people list
page["#memberships"].html(render(:partial => "memberships/memberships.html.haml", 
                                 :locals => {:memberships => @memberships, :non_members => @non_members}))
