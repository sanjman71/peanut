- title("Compose Message")
- javascript('messages')

.span-23.prepend-1.mat.padding-top-10.bottom
  %h2== Compose Message
  %h4= link_to 'Cancel', messages_path, :class => 'admin color'

%hr.space

.prepend-1.last
  -# add a waitlist message hidden form
  = render(:partial => 'messages/send_message', :locals => {:sender => current_user, :protocol => 'email', :recipient => @recipient})
