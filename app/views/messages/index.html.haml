- title("Messages")

.span-23.prepend-1.mat.padding-top-10.bottom
  %h2== Messages
  %h4
    %span{:style => 'padding-right: 7px;'}== Total: #{@total_count}
    - @msgs_by_protocol.keys.each do |protocol|
      %span{:style => 'padding-right: 7px;'}== #{protocol.titleize}: #{@msgs_by_protocol[protocol]}
  %h4
    = link_to 'New Message', new_message_path, :class => 'admin'

%hr.space

.span-14.prepend-1
  = display_flash(:all)

%hr.space

-# column headers
%h4.span-20.last
  .span-4.prepend-1#sender_column
    %h3.bottom== From
  .span-8#subject_column
    %h3.bottom== Subject
  .span-7.last#when_column
    %h3.bottom== Sent

%hr.space

- @messages.each do |message|
  .span-20.last.letterbox.message{:class => cycle("shaded", "")}[message]
    .span-4.prepend-1#sender
      %h4= message.sender.name
    .span-8#subject
      %h4= message.subject
    .span-7.last#when
      %h4= message.updated_at.to_s(:appt_day_date_time)