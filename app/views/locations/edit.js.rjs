page.replace :locations, :partial => "locations/edit_location", :object => @location, 
  :locals => {
      :form_path => location_path(@location), 
      :form_method => :put, 
      :form_submit => 'Update'
  }
