= link_to('Home', root_path(:subdomain => current_subdomain), :class => 'signup')
== &nbsp;|&nbsp;

- if has_privilege?('read companies')
  = link_to('Companies', companies_path(:subdomain => current_subdomain), :class => 'signup')
  == &nbsp;|&nbsp;

- if has_privilege?('create roles privileges') || has_privilege?('read roles privileges') || has_privilege?('update roles privileges') || has_privilege?('delete roles privileges')
  = link_to('Privileges & Roles', badges_path, :class => 'signup')
  == &nbsp;|&nbsp;
  
-# everyone sees the sign-up link
= link_to('Sign Up', signup_path, :class => 'signup')
== &nbsp;|&nbsp;

- if logged_in?
  = link_to('Log-out', logout_path, :class => 'signup')
- else
  = link_to('Log-in', login_path, :class => 'signup')
  
- if RAILS_ENV == 'development'
  == &nbsp;|&nbsp;
  = link_to('Toggle Grid', url_for(:controller => 'debug', :action => 'toggle_blueprint_grid'), :method => :put, :class => 'signup')

