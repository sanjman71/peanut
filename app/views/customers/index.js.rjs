# update customer search text
page["#customer_search_text"].html(@search_text)

# update customer list
page["#customers"].html(render(:partial => 'customers.html.haml', :locals => {:customers => @customers}))