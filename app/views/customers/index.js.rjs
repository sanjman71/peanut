# update customer search text
page[:customer_search_text].replace_html @search_text

# update customer list
page[:customers_table].replace_html :partial => 'customers.html.haml', :locals => {:customers => @customers}
