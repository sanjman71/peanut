.span-7.prepend-1.append-bottom.customers.header
  %h4.bottom= search_text ? search_text : page_entries_info(customers, :entry_name => 'customer')
.span-8.last#pagination
  - if paginate
    = will_paginate(customers, :page_links => true)
