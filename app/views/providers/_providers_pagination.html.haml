.span-10.prepend-1.append-bottom.providers.header
  %h4.bottom= search_text ? search_text : page_entries_info(providers, :entry_name => 'provider')
.span-4.prepend-1.last#pagination
  - if paginate
    = will_paginate(providers)
