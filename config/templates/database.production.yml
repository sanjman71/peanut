# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
production:
  encoding: utf8
  adapter: mysql
  host: localhost
  port: 3306
  database: peanut_production
  username: rails
  password: rails
